Internet protocol ip
Позволяет отправлять пакеты между компьютерами ничего не грарантирует

tcp много чего гарантирует - надежная передача данных

Как выглядит адресс - 3 части
1) адресс комрьютера ip
2) номер протокола tcp, ip, ethernet... в рамках которого происходит доставка
3) порт


Качество передачи

Надежность данных:
1) все отправляемые данные до получателя
2) нет лишних
3) не изменяются
4)
5)

Что должно входить в tcp заголовок
TCP:
scr port
dst port
crc контроьлная сумма пакета
len длина пакета
seq - sequence number - номер начала пакета внутри потока. удобно нумеровать байты в потоке -  первый номер в последовательности байтов в потоке
seq_ack - acknolegment
flag			
			WINDOW - ограничение объема памяти + ограничения для макс производительности

MultiStream TCP

Для установления соединения - пакет с флагом SYN
*->SYN->*   *<-SYN, ACK<-*	*->ACK->*

seq=1000 - байты
lem=1000
*->FIN->**		2000 - подствердим получение данных, надо 2001(так как fin рамером в байт(так считается))
*<-<-*

Все можно посмотреть в tcpdump



IP:
IP SRC
IP DST
fragment num
признак того, что пакет последний
время жизни TTL
CRC контролььная сумма
proto номер протокола



UDP - без установки соединения просто передать данные
src port
dst port
crc 